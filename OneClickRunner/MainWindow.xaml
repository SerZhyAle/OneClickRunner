<Window x:Class="OneClickRunner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OneClickRunner"
        mc:Ignorable="d"
        Title="OneClickRunner Settings" Height="500" Width="700"
        Closing="Window_Closing">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Applications and Scripts" FontSize="16" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>

        <ListView Name="AppListView" Grid.Row="1" SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Path" Width="350" DisplayMemberBinding="{Binding Path}"/>
                    <GridViewColumn Header="Arguments" Width="120" DisplayMemberBinding="{Binding Arguments}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Name="AddButton" Content="Add" Width="80" Margin="0,0,10,0" Click="AddButton_Click"/>
            <Button Name="ImportButton" Content="Import" Width="80" Margin="0,0,10,0" Click="ImportButton_Click"/>
            <Button Name="RunButton" Content="Run" Width="80" Margin="0,0,10,0" Click="RunButton_Click"/>
            <Button Name="EditButton" Content="Edit" Width="80" Margin="0,0,10,0" Click="EditButton_Click"/>
            <Button Name="RemoveButton" Content="Remove" Width="80" Click="RemoveButton_Click"/>
        </StackPanel>

        <GroupBox Header="Autostart Settings" Grid.Row="3" Padding="10">
            <StackPanel>
                <CheckBox Name="AutostartCheckBox" Content="Start OneClickRunner when Windows starts" 
                          Checked="AutostartCheckBox_Changed" Unchecked="AutostartCheckBox_Changed"/>
                <TextBlock Text="The application will run minimized." 
                           Margin="20,5,0,0" FontSize="11" Foreground="Gray"/>
                <Button Name="ExitButton" Content="Exit Application" Width="120" Margin="0,10,0,0" HorizontalAlignment="Left" Click="ExitButton_Click"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
