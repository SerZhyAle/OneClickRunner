# AGENTS.md

The always-loaded agent contract for this repo lives in [`CLAUDE.md`](CLAUDE.md) — Claude
Code's native filename. The content is tool-neutral: if your tool reads `AGENTS.md` (OpenAI
Codex and others), read `CLAUDE.md` in full and treat it as the project's agent contract.

Keep exactly **one** canonical rules file (`CLAUDE.md`). Do not fork the rules here.
